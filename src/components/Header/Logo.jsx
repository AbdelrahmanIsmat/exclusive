import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/HomePage" className="text-2xl font-Inter font-bold">
      Exclusive
    </Link>
  );
};

export default Logo;
